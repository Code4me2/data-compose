# Docker Compose configuration for n8n workflow automation with custom nodes
# This setup includes a complete environment with n8n, web server, and database

version: '3.8'  # Using recent compose version for better feature support

services:
  # n8n workflow automation engine
  n8n:
    image: n8nio/n8n:latest
    restart: always
    ports:
      - "5678:5678"  # Expose n8n web interface to host
    environment:
 
