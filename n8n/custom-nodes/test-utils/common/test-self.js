#!/usr/bin/env node

// Self-test for the test runner
console.log('Test Runner Self-Test');
console.log('Node version:', process.version);
console.log('Current directory:', process.cwd());

// This should exit with code 0 for success
process.exit(0);